# Deploying
https://docs.flutter.dev/deployment/android

### Versioning
See the `version` option in _pubspec.yaml_. The +x suffix is the Android version code and must be incremented to be unique for each new deployed build.

### Building an APL
Run `flutter build apk`.

### To-Do
Keep screen awake
App must target Android 15 (API level 35) or higher
